<h1>Editing Book</h1>

<%= form_with model:@book, local:true do |f| %>
    <div>Title</h5>
    <%= f.text_field :title %>
    <h5>Body</h5>
    <%= f.text_area :body %>
    <%= f.submit 'Update book' %>
<% end %>
